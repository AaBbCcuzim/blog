# CSS

```
* {
    padding: 0;
    margin: 0;
}
```

## 选择器

### 基础选择器

#### 标签选择器

**使用 HTML 标签名称作为选择器，为页面中某一类标签指定统一的 CSS 样式**

```
标签名 {
    属性: 属性值;
    ...
}
```

#### 类选择器

**使用 HTML 中 class 属性作为选择器，为页面中某一类标签指定统一的 CSS 样式**

```
.类名 {
    属性: 属性值;
    ...
}
```

> **类命名规范：**
>
> - **头： head**
> - **内容：content/container**
> - **尾： footer**
> - **导航：nav**
> - **侧栏： sidebar**
> - **栏目： column**
> - **页面外围控制整体布局宽度：wapper**
> - **菜单：menu**

#### id 选择器

**使用 HTML 中 id 属性作为选择器，为页面中某一类标签指定统一的 CSS 样式**

```
#id名 {
属性: 属性值;
    ...
}
```

- **一个 id 选择器在一个 HTML 文档中只能调用一次**

#### 通配符选择器

**使用\*选取页面中所有元素**

```
* {
    属性: 属性值;
    ...
}
```

### 复合选择器

#### 后代选择器

**选择父元素中的子元素**

```
元素1 元素2 {
    
}
```

- **元素 1 和元素 2 中间用空格隔开**
- **元素 1 是父级，元素 2 是子级，最终选择的是元素 2**

#### 子选择器

**选择元素的最近一级子元素**

```
元素1>元素2 {

}
```

- **元素 1 和元素 2 中间用>隔开**
- **元素 1 是父级，元素 2 是子级，最终选择的是元素 2**
- **元素 2 必须是亲儿子**

#### 并集选择器

**选择多组标签定义相同样式**

```
元素1,元素2 {
    
}
```

- **元素 1 和元素 2 中间用逗号隔开**

#### 伪类选择器

**用于向某些选择器添加特殊的效果**

```
selector:pseudo-class {
    property: value;
}
```

| **参数**     | **说明**                   |
| ------------ | -------------------------- |
| **:link**    | **选择所有未被访问的**     |
| **:visited** | **选择所有已被访问的**     |
| **:hover**   | **选择鼠标指针位于其上的** |
| **:active**  | **选择鼠标按下未弹起的**   |
| **:focus**   | **选取获取焦点的**         |

### CSS3 新增选择器

#### 属性选择器

**根据元素特定属性选择元素**

| **选择符**        | **简介**                                        |
| ----------------- | ----------------------------------------------- |
| **E[att]**        | **选择具有 att 属性的 E 元素**                  |
| **E[att="val"]**  | **选择具有 att 属性且属性值等于 val 的 E 元素** |
| **E[att^="val"]** | **匹配具有 att 属性且值以 val 开头的 E 元素**   |
| **E[att$="val"]** | **匹配具有 att 属性且值以 val 结尾的 E 元素**   |
| **E[att*="val"]** | **匹配具有 att 属性且值中含有 val 的 E 元素**   |

#### 结构伪类选择器

**根据文档结构来选择器元素**

| **选择符**          | **简介**                          |
| ------------------- | --------------------------------- |
| **E:first-child**   | **匹配父元素中的第一个子元素 E**  |
| **E:last-child**    | **匹配父元素中的最后一个元素 E**  |
| **E:nth-child(n)**  | **匹配父元素中的第 n 个子元素 E** |
| **E:first-of-type** | **指定类型 E 的第一个**           |
| **E:last-of-type**  | **指定类型 E 的最后一个**         |
| **E:nth-of-type**   | **指定类型 E 的第 n 个**          |

- **nth-child 对父元素里面所有孩子排序选择（序号是固定的）先找到第 n 个孩子，然后看看是否和 E 匹配**
- **nth-of-type 对父元素里面指定子元素进行排序。先匹配 E，然后再根据 E 找第 n 个孩子**

#### 伪元素选择器

**利用 CSS 创建新标签元素，而不需要 HTML 标签，从而简化 HTML 结构**

| **选择符**   | **简介**                     |
| ------------ | ---------------------------- |
| **::before** | **在元素内部的前面插入内容** |
| **::after**  | **在元素内部的后面插入内容** |

- **before 和 after 创建一个元素，但是属于行内元素**
- **新创建的这个元素在文档树中是找不到的，所以我们称为伪元素**
- **before 和 after 必须有 content 属性**
- **before 在父元素内容的前面创建元素，after 在父元素内容的后面插入元素**
- **伪元素选择器和标签选择器一样，权重为 1**

### 选择器权重

| **选择器**               | **选择器权重** |
| ------------------------ | -------------- |
| **继承或者\***           | **0,0,0,0**    |
| **元素选择器**           | **0,0,0,1**    |
| **类选择器，伪类选择器** | **0,0,1,0**    |
| **ID 选择器**            | **0,1,0,0**    |
| **行内样式 style=""**    | **1,0,0,0**    |
| **!important 重要的**    | **无穷大**     |

## 私有前缀

- **-moz-：代表 firefox 浏览器私有属性**
- **-ms-：代表 ie 浏览器私有属性**
- **-webkit-：代表 safari,chrome 私有属性**
- **-o-：代表 Oprea 私有属性**

## width

**宽度**

> **calc()可以进行一些简单的属性值计算，如 width: calc(100% - 80px);**

## height

**高度**

## font

**字体**

### font-family

**定义文本的字体系列**

- **各个字体之间使用英文逗号隔开**
- **有空格隔开的多个单词组成的字体，加引号**

### font-size

**定义字体大小**

- **body 指定文字大小对标题标签无效，需单独指定**

### font-weight

**定义字体粗细**

| **参数名**  | **参数说明**                                  |
| ----------- | --------------------------------------------- |
| **normal**  | **正常字体。相当于 number 为 400 （默认值）** |
| **bold**    | **粗体。相当于 number 为 700**                |
| **bolder**  | **特粗体**                                    |
| **lighter** | **细体**                                      |
| **number**  | **100**                                       |

### font-style

**定义字体样式**

| **属性值** | **作用**                                |
| ---------- | --------------------------------------- |
| **normal** | **默认值 ，浏览器会显示标准的字体样式** |
| **italic** | **浏览器会显示斜体的字体样式**          |

> **复合属性：**
>
> ```
> body {
> font: font-style font-weight font-size/line-height font-family;
> }
> ```
>
> - **属性不能更换顺序，空格隔开**
> - **不需要的属性可以省略，但必须保留 font-size 和 font-family，否则 font 将不起作用**

## color

**定义文本的颜色**

| **表示**       | **属性值**                       |
| -------------- | -------------------------------- |
| **预定义颜色** | **red,green**                    |
| **十六进制**   | **#FF0000**                      |
| **RGB 代码**   | **rgb(255,0,0),rgb(100%,0%,0%)** |

## text

**文本**

### text-align

**设置元素内文本内容的\*\***水平\*\*对齐方式

| **属性值** | **解释**           |
| ---------- | ------------------ |
| **left**   | **左对齐（默认）** |
| **right**  | **右对齐**         |
| **center** | **居中对齐**       |

> **行内元素或行内块元素水平居中：**
>
> - **给父元素添加 text-align:center**

### text-decoration

**定义添加到文本的装饰**

| **属性值**       | **描述**            |
| ---------------- | ------------------- |
| **none**         | **默认 没有装饰线** |
| **underline**    | **下划线**          |
| **overline**     | **上划线**          |
| **line-through** | **删除线**          |

### text-indent

**指定文本的第一行缩进**

- **em 是一个相对单位，1em 就是当前元素（font-size）一个文字的大小**

### line-height

**设置行间的距离**

- **行间距由上间距，文本高度，下间距构成，文本高度不变，实际改变的是上下间距**
- **行高设置成盒子高度可以保持文本垂直居中**

### text-shadow

**设置文字的阴影**

```
text-shadow: h-shadow v-shadow biur color;
```

| **值**       | **描述**                           |
| ------------ | ---------------------------------- |
| **h-shadow** | **必需。水平阴影的位置。允许负值** |
| **v-shadow** | **必需。垂直阴影的位置。允许负值** |
| **blur**     | **可选。模糊距离。**               |
| **color**    | **可选。阴影的颜色。**             |

### text-overflow

**确定如何提示用户存在隐藏的溢出内容**

| **值**       | **描述**                               |
| ------------ | -------------------------------------- |
| **clip**     | **默认值。在内容区域的极限处截断文本** |
| **ellipsis** | **省略号**                             |

## display

**规定元素应该生成的框的类型**

| **值**           | **描述**                                                 |
| ---------------- | -------------------------------------------------------- |
| **none**         | **此元素不会被显示。**                                   |
| **block**        | **此元素将显示为块级元素，此元素前后会带有换行符。**     |
| **inline**       | **默认。此元素会被显示为内联元素，元素前后没有换行符。** |
| **inline-block** | **行内块元素。**                                         |
| **-webkit-box**  | **弹性伸缩盒子模型**                                     |

- **display 隐藏元素后，不再占有原来的位置**

## background

**背景**

### background-color

**定义元素的背景颜色**

| **参数**        | **说明**         |
| --------------- | ---------------- |
| **transparent** | **透明。默认值** |
| **color**       | **颜色**         |

> **背景颜色半透明：**
>
> ```
> background: rgba( , , , );
> ```
>
> - **最后一个参数 alpha 透明度，取值范围在 0~1 之间**

### background-image

**定义元素的背景图片**

| **参数** | **说明**               |
| -------- | ---------------------- |
| **none** | **无背景图**           |
| **url**  | **背景图的路径 url()** |

### background-repeat

**背景平铺**

| **参数**      | **说明**          |
| ------------- | ----------------- |
| **no-repeat** | **不平铺**        |
| **repeat**    | **平铺。默认**    |
| **repeat-x**  | **沿着 x 轴平铺** |
| **repeat-y**  | **沿着 y 轴平铺** |

### background-position

**定义元素的背景图片的位置**

| **参数**     | **说明**   |
| ------------ | ---------- |
| **length**   | **百分数** |
| **position** | **top**    |

- **两个参数分别代表 x,y**

### background-attachment

**背景图像固定**

| **参数**   | **说明**                   |
| ---------- | -------------------------- |
| **scroll** | **背景图像随对象内容滚动** |
| **fixed**  | **背景图像固定**           |

### background-size

**背景图像的尺寸**

- **单位：长度 | 百分比 | cover | contain**
- **cover 把背景图像扩展至足够大，以使背景图像完全覆盖背景区域**
- **contain 把图像扩展至最大尺寸，以使其宽度和高度完全适应内容区域**

## border

**边框**

### border-top

**只设定上边框**

### border-left

**只设定左边框**

### border-bottom

**只设定下边框**

### border-right

**只设定右边框**

### border-width

**边框的粗细**

### border-style

| **参数**   | **说明**     |
| ---------- | ------------ |
| **none**   | **无边框**   |
| **soild**  | **实线边框** |
| **dashed** | **虚线边框** |

### border-color

**边框颜色**

### border-collapse

**合并相邻的边框**

| **参数**     | **说明**         |
| ------------ | ---------------- |
| **collapse** | **合并相邻边框** |

### border-radius

**边框的圆角**

### border-top-left-radius

**边框的左上圆角**

### border-top-right-radius

**边框的右上圆角**

### border-bottom-right-radius

**边框的右下圆角**

### border-bottom-left-radius

**边框的左下圆角**

## padding

**内边框**

- **盒子本身没有指定 width/height 属性,padding 不会撑开盒子大小**

### padding-top

**上内边距**

### padding-left

**左内边距**

### padding-bottom

**下内边距**

### padding-right

**右内边距**

## margin

**外边距**

### margin-top

**上外边距**

### margin-left

**左外边距**

### margin-bottom

**下外边距**

### margin-right

**右外边距**

> **块级盒子水平居中：**
>
> 1. **盒子必须指定宽度(width)**
> 2. **盒子\*\***左右外边距\*\*都设置为 auto
>
> **嵌套块元素垂直外边距的塌陷：**
>
> - **可以为父元素定义上边框**
> - **可以为父元素定义上内边距**
> - **可以为父元素添加 overflow:hidden**

## list

**列表**

### list-style

**列表项目符号样式**

| **参数** | **说明**         |
| -------- | ---------------- |
| **none** | **去掉项目符号** |

## box

**盒子**

### box-shadow

**盒子阴影**

```
box-shadow: h-shadow v-shadow blur spread color inset;
```

| **值**       | **描述**                                     |
| ------------ | -------------------------------------------- |
| **h-shadow** | **必需。水平阴影的位置。允许负值**           |
| **v-shadow** | **必需。垂直阴影的位置。允许负值**           |
| **blur**     | **可选。模糊距离。**                         |
| **spread**   | **可选。阴影的尺寸。**                       |
| **color**    | **可选。阴影的颜色。**                       |
| **inset**    | **可选。将外部阴影（outset）改为内部阴影。** |

- **默认的是外阴影，但是不可以写这个单词，否则导致阴影无效**

### box-sizing

**指定盒模型**

| **属性值**      | **描述**                                  |
| --------------- | ----------------------------------------- |
| **content-box** | **盒子大小为 width+padding+border。默认** |
| **border-box**  | **盒子大小为 width**                      |

## filter

**滤镜**

| **属性值**       | **描述**                     |
| ---------------- | ---------------------------- |
| **blur()**       | **模糊处理，数值越大越模糊** |
| **brightness()** | **明亮处理**                 |
| **contrast()**   | **调整输入图像的对比度**     |
| **grayscale()**  | **改变输入图像灰度**         |
| **opacity()**    | **转化图像的透明程度**       |

## float

**浮动**

**float 属性用于创建浮动框，将其移动到一边，直到左边缘或右边缘触及包含快或另一个浮动框的边缘**

| **属性值** | **描述**                 |
| ---------- | ------------------------ |
| **none**   | **元素不浮动（默认值）** |
| **left**   | **元素向左浮动**         |
| **right**  | **元素向右浮动**         |

- **脱离标准普通流的控制（浮）移动到指定位置（动），（俗称脱标）**
- **浮动的盒子不再保留原先的位置**

> **清除浮动的办法：**
>
> - **额外标签法** > **在浮动元素末尾添加一个空的标签。例如**<**div style="clear:both">**<**/div>**
> - **父级添加 overflow 属性** > **给父元素添加 overflow 属性，将其属性值设置 hideen,auto 或 scroll**
> - **父级添加 after 伪元素**
>   ```
>   .clearfix:after {
>       content: "";
>       display: block;
>       height: 0;
>       clear: both;
>       visibility: hidden;
>   }
>   .clearfix {
>       /*IE6、7专有*/
>       *zoom: 1;
>   }
>   ```
> - **父级添加双伪元素**
>
>   ```
>   .clearfix:before,
>   .clearfix:after {
>       content: "";
>       display: table;
>   }
>
>   .clearfix:after {
>       clear: both;
>   }
>
>   .clearfix {
>       *zoom: 1;
>   }
>   ```

## clear

**清除**

| **属性值** | **描述**                                       |
| ---------- | ---------------------------------------------- |
| **left**   | **不允许左侧有浮动元素（清除左侧浮动的影响）** |
| **right**  | **不允许右侧有浮动元素（清除右侧浮动的影响）** |
| **both**   | **同时清除左右两侧浮动的影响**                 |

## overflow

**设置元素溢出的行为**

| **属性值**  | **描述**                                                                                                                              |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **visible** | **内容不能被裁减并且可能渲染到边距盒（padding）的外部**                                                                               |
| **hidden**  | **内容将被裁减以适应边距（padding）盒**                                                                                               |
| **scroll**  | **内容将被裁减以适应边距（padding）盒并显示滚动条**                                                                                   |
| **auto**    | **如果内容适应边距（padding）盒，它看起来与** `visible`相同，但是仍然建立了一个新的块级格式化上下文。如果内容溢出，则浏览器提供滚动条 |

## positon

**位置**

| **属性值**   | **描述**                               |
| ------------ | -------------------------------------- |
| **static**   | **静态定位。默认。无定位**             |
| **relative** | **相对定位。相对元素原本的位置移动**   |
| **absolute** | **绝对定位。相对元素的祖先元素**       |
| **fixed**    | **固定定位。固定于浏览器可视区**       |
| **sticky**   | **黏性定位。相对定位和固定定位的混合** |

**相对定位：**

- **占有原先的位置**

**绝对定位：**

- **如果没有祖先元素或者祖先元素没有定位，则以浏览器为准定位**
- **如果祖先元素有定位（相对，绝对，固定定位），则以最近一级的有定位祖先元素为参考点移动位置**
- **不在占有原先的位置**

**固定定位：**

- **以浏览器的可视窗口为参照点移动元素**
- **跟父元素没有任何关系**
- **不随滚动条滚动**
- **不在占有原先的位置**

**黏性定位：**

- **以浏览器的可视窗口为参照点移动元素**
- **粘性定位占有原先的位置**

## top

**顶端偏移量，定义元素相对于其父元素\*\***上边线的距离\*\*

## bottom

**底部偏移量，定义元素相对于其父元素\*\***下边线的距离\*\*

## left

**左侧偏移量，定义元素相对于其父元素\*\***左边线的距离\*\*

## right

**右侧偏移量，定义元素相对于其父元素\*\***右边线的距离\*\*

## z-index

**定义盒子重叠时的前后顺序**

- **数值可以是正整数、负整数或 0，默认是 auto，数值越大，盒子越靠上**
- **如果属性值相同，则按照书写顺序，后来居上**
- **数字后面不能加单位**
- **只有定位的盒子才有 z-index 属性**

## visibility

**可见性**

| **属性值**  | **描述**     |
| ----------- | ------------ |
| **visible** | **元素可视** |
| **hidden**  | **元素隐藏** |

- **visibility 隐藏元素后，继续占有原来的位置**

## cursor

**光标**

| **属性值**      | **描述**      |
| --------------- | ------------- |
| **default**     | **小白 默认** |
| **pointer**     | **小手**      |
| **move**        | **移动**      |
| **text**        | **文本**      |
| **not-allowed** | **静止**      |

## outline

**轮廓**

### outline-style

**轮廓样式**

| **属性值** | **描述**   |
| ---------- | ---------- |
| **none**   | **无轮廓** |
| **solid**  | **实线**   |

## resize

**控制一个元素的可调整大小性**

| **属性值**     | **描述**                                     |
| -------------- | -------------------------------------------- |
| **none**       | **元素不能被用户缩放**                       |
| **both**       | **允许用户在水平和垂直方向上调整元素的大小** |
| **horizontal** | **允许用户在水平方向上调整元素的大小**       |
| **vertical**   | **允许用户在垂直方向上调整元素的大小**       |

## vertical

### vertical-align

**指定行内元素（inline）或表格单元格（table-cell）元素的垂直对齐方式**

| **值**       | **描述**                                   |
| ------------ | ------------------------------------------ |
| **baseline** | **默认。元素放置在父元素的基线上**         |
| **top**      | **使单元格内边距的上边缘与该行顶部对齐。** |
| **middle**   | **把此元素放置在父元素的中部**             |
| **bottom**   | **使单元格内边距的下边缘与该行底部对齐。** |

## white

### white-space

**设置如何处理元素中的空白**

| **值**     | **描述**                                                           |
| ---------- | ------------------------------------------------------------------ |
| **normal** | **自动换行。连续的空白符会被合并，换行符会被当作空白符来处理。**   |
| **nowrap** | **换行无效。连续的空白符会被合并。**                               |
| **pre**    | **连续的空白符会被保留。在遇到换行符或者**<**br>元素时才会换行。** |

## -webkit

### -webkit-line-clamp

**定义内容限制为指定的行数**

### -webkit-box-orient

**设置或检索伸缩盒对象的子元素的排列方式**

### -webkit-box-reflect

**设置倒影特效**

- | **direction 定义方向** | **属性值**               | **描述** |
  | ---------------------- | ------------------------ | -------- |
  | **above**              | **指定倒影在对象的上边** |
  | **below**              | **指定倒影在对象的下边** |
  | **left**               | **指定倒影在对象的左边** |
  | **right**              | **指定倒影在对象的右边** |
- **offset 定义反射偏移的距离，取值包括数值或百分比，其中百分比根据对象的尺寸进行确定。默认为 0。**
  **用长度值来定义倒影与对象之间的间隔。可以为负值。用百分比来定义倒影与对象之间的间隔。可以为负值。**
- | **mask-box-image 定义遮罩图像，该图像将覆盖投影区域。如果省略该参数值，则默认为无遮罩图像。** | **属性值**                                 | **描述** |
  | --------------------------------------------------------------------------------------------- | ------------------------------------------ | -------- |
  | **none**                                                                                      | **无遮罩图像**                             |
  | **linear-gradient**                                                                           | **使用线性渐变创建遮罩图像**               |
  | **radial-gradient**                                                                           | **使用径向(放射性)渐变创建遮罩图像**       |
  | **repeating-linear-gradient**                                                                 | **使用重复的线性渐变创建背遮罩像**         |
  | **repeating-radial-gradient**                                                                 | **使用重复的径向(放射性)渐变创建遮罩图像** |

## content

**用于在元素的伪元素中插入内容**

## transition

**过渡**

```
transition:要过渡的元素 花费时间 运动曲线 何时开始
```

- **属性：想要变化的 CSS 属性，宽度高度背景颜色内外边距都可以。如果想要所有的属性都变化过渡，写一个 all 就可以**
- **花费时间：单位是秒（必须写单位）比如 0.5s**
- **运动曲线：默认是 ease（可以省略）**

| **属性值**      | **描述**         |
| --------------- | ---------------- |
| **linear**      | **匀速**         |
| **ease**        | **逐渐慢下来**   |
| **ease-in**     | **加速**         |
| **ease-out**    | **减速**         |
| **ease-in-out** | **先加速后减速** |

- **何时开始：单位是秒（必须写单位）可以设置延迟触发时间 默认是 0s（可以省略）**

## transform

**转换**

| **属性值**              | **描述**              |
| ----------------------- | --------------------- |
| **translate(x,y)**      | **移动**              |
| **translateX(n)**       | **沿 x 轴移动**       |
| **translateY(n)**       | **沿 y 轴移动**       |
| **rotate(度数)**        | **旋转**              |
| **scale(x,y)**          | **缩放**              |
| **translateZ(n)**       | **沿 z 轴移动**       |
| **translate3d(x,y,z)**  | **移动**              |
| **rotateX**             | **沿 x 轴正方向旋转** |
| **rotateY**             | **沿 y 轴正方向旋转** |
| **rotateZ**             | **沿 z 轴正方向旋转** |
| **rotate3d(x,y,z,deg)** | **沿自定义轴旋转**    |

- **translate 不会影响到其他元素的位置**
- **translate 对行内标签没有效果**
- **rotate 里面跟度数，单位是 deg**
- **角度为正时，顺时针，负时，逆时针**
- **默认旋转的中心点是元素的中心点**
- **scale 不影响其他盒子**

### transform-origin

**设置元素转换的中心点**

- **x y 默认转换的中心点是元素的中心点（50% 50%）**
- **还可以给 x y 设置元素或者方位名词（top bottom left right center）**

### transform-style

**控制子元素是否开启三维立体环境**

| **属性值**   | **描述**                     |
| ------------ | ---------------------------- |
| **flat**     | **默认。不开启 3d 立体空间** |
| **preserve** | **开启 3d 立体空间**         |

- **代码写给父盒子，影响子盒子**

## @keyframes

**定义动画**

```
@keyframes 动画名称 {
    0% {
        
    }
    100% {
        
    }
}
```

- **0%是动画的开始，100%是动画的完成**
- **0%和 100%可由 from 和 to 替换**
- **动画是使元素从一种样式逐渐变化为另一种样式的效果，可以改变任意多的样式任意多的次数**

## animation

**调用动画**

```
animation: 动画名称 持续时间 运动曲线 何时开始 播放次数 是否反方向 动画起始或者结束的状态
```

### animation-name

**动画名称**

### animation-duration

**动画持续时间**

### animation-timing-fuction

**动画的速度曲线**

| **属性值**      | **描述**         |
| --------------- | ---------------- |
| **linear**      | **匀速**         |
| **ease**        | **逐渐慢下来**   |
| **ease-in**     | **加速**         |
| **ease-out**    | **减速**         |
| **ease-in-out** | **先加速后减速** |
| **steps()**     | **几步完成**     |

### animation-delay

**动画何时开始**

### animation-iteration-count

**动画被播放的次数**

| **属性值**   | **描述**               |
| ------------ | ---------------------- |
| **数字**     | **播放对应数字的次数** |
| **infinite** | **无限循环**           |

### animation-direction

**规定动画是否在下一周期逆向播放**

| **属性值**    | **描述**         |
| ------------- | ---------------- |
| **normal**    | **默认。不逆向** |
| **alternate** | **逆向**         |

### animation-play-state

**规定动画是否正在运行或暂停**

| **属性值**  | **描述** |
| ----------- | -------- |
| **running** | **运行** |
| **pause**   | **停止** |

### animation-fill-mode

**动画结束后状态**

| **属性值**    | **描述**               |
| ------------- | ---------------------- |
| **backwards** | **默认。返回起始状态** |
| **forwards**  | **t 停在结束状态**     |

## perspective

**透视**

- **透视写在被观察元素的父盒子上面**

## linear

### linear-gradient

**渐变颜色**

```
linear-gradient(起始方向，颜色1，颜色2，...)
```

- **背景渐变必须添加浏览器私有前缀**
- **起始方向可以是：方位名词或者度数，如果省略默认就是 top**

## flex

**布局原理：通过给父盒子添加 flex 属性，来控制子盒子的位置和排列方式**

![image-20230314114903845](file:///C:/Users/AaBbCcuzim/Desktop/md/image-20230314114903845.png?lastModify=1711791236)

## flex-direction

**设置主轴的方向**

| **属性值**         | **说明**               |
| ------------------ | ---------------------- |
| **row**            | **从左到右（默认值）** |
| **row-reverse**    | **从右到左**           |
| **column**         | **从上到下**           |
| **column-reverse** | **从下到上**           |

- **主轴和侧轴是会变化的，就看** `flex-direction`设置谁为主轴，剩下的就是侧轴，而子元素就是跟着主轴来排列的

## justify-content

**设置主轴上的子元素排序方式**

| **属性值**        | **说明**                                             |
| ----------------- | ---------------------------------------------------- |
| **flex-start**    | **从头部开始 如果主轴是 x 轴，则从左到右（默认值）** |
| **flex-end**      | **从尾部开始排列**                                   |
| **center**        | **在主轴居中对齐（如果主轴是 x 轴则居中对齐）**      |
| **space -around** | **平分剩余空间**                                     |
| **space-between** | **先两边贴边 再平分剩余空间**                        |

- **使用这个属性之前一定要确定好主轴是哪个**

## flex-warp

**设置子元素是否换行**

| **属性值** | **说明**          |
| ---------- | ----------------- |
| **nowarp** | **默认值 不换行** |
| **wrap**   | **换行**          |

- **默认情况下，项目都在一条线上，**`flex-wrap`属性定义，flex 布局中默认是不换行的

## align-items

**设置侧轴上的子元素排列方式（单行）**

| **属性值**     | **说明**                                             |
| -------------- | ---------------------------------------------------- |
| **flex-start** | **从头部开始 如果主轴是 x 轴，则从上到下（默认值）** |
| **flex-end**   | **从尾部开始排列**                                   |
| **center**     | **在侧轴居中对齐（如果主轴是 x 轴则垂直居中对齐）**  |
| **stretch**    | **拉伸（默认值）**                                   |

## align-content

**设置侧轴上的子元素的排列方式（多行）**

| **属性值**        | **说明**                                   |
| ----------------- | ------------------------------------------ |
| **flex-start**    | **在侧轴的头部开始排列（默认值）**         |
| **flex-end**      | **在侧轴的尾部开始排列**                   |
| **center**        | **在侧轴中间显示**                         |
| **space-around**  | **子项在侧轴平分剩余空间**                 |
| **space-between** | **子项在侧轴先分布在两头，再平分剩余空间** |
| **stretch**       | **设置子项元素高度平分父元素高度**         |

- **设置子项在侧轴上的排列方式 并且只能用于子项出现**换行**的情况（多行）,在单行下是没有效果的**

## flex-flow

`flex-direction`和 `flex-wrap`属性的复合属性

```
flex-flow: row wrap;
```

## flex:

**定义子项目分配剩余空间，用 flex 来表示占多少份数**

```
div {
    flex: <number> /* default 0 */
}
```

## align-self

**控制子项自己在侧轴上的排列方式**

| **属性值**     | **说明**                                            |
| -------------- | --------------------------------------------------- |
| **auto**       | **（默认值）**                                      |
| **flex-start** | **从头部开始 如果主轴是 x 轴，则从上到下**          |
| **flex-end**   | **从尾部开始排列**                                  |
| **center**     | **在侧轴居中对齐（如果主轴是 x 轴则垂直居中对齐）** |
| **stretch**    | **拉伸（默认值）**                                  |

`align-self`属性允许单个项目有与其他项目不一样的对齐方式，可覆盖 `align-items`属性。默认值为 `auto`，表示继承父元素的 `align-items`属性，如果没有父元素，则等同于 `stretch`

## order

**定义项目的排列顺序**

- **数值越小，排列越靠前，默认为 0**

## @media

**媒体查询**

- **使用@media 查询，可以针对不同的媒体类型定义不同的样式**
- **@media 可以针对不同的屏幕尺寸设置不同的样式**
- **当你重置浏览器大小的过程中，页面也会根据浏览器的宽度和高度重新渲染页面**

```
@media mediatype and|not|only (media feature) {
    CSS-Code;
}
```

- **使用** `@media`开头 注意@符号
- `mediatype` 媒体类型
- **关键字 **`and not only`
- `media feature`必须有小括号包含

**mediatype**

| **值**     | **解释说明**                           |
| ---------- | -------------------------------------- |
| **all**    | **用于所有设备**                       |
| **print**  | **用于打印机和打印预览**               |
| **screen** | **用于电脑屏幕，平板电脑，智能手机等** |

**关键字**

| **值**   | **解释说明**                                 |
| -------- | -------------------------------------------- |
| **and**  | **可以将多个媒体特性连接到一起，相当于“且”** |
| **not**  | **排除某个媒体类型，相当于“非”的意思**       |
| **only** | **指定某个特定的媒体类型，可以省略**         |

**媒体特性**

| **值**        | **解释说明**                             |
| ------------- | ---------------------------------------- |
| **width**     | **定义输出设备中页面可见区域的宽度**     |
| **min-width** | **定义输出设备中页面最小可见区域的宽度** |
| **max-width** | **定义输出设备中页面最大可见区域的宽度** |
